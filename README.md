# WinMan - Windows User Management CLI Tool

WinMan is a Python command-line interface for managing Windows local user accounts and groups using PowerShell commands and built-in Windows utilities. It enables IT administrators and power users to automate user account operations with an easy-to-use interactive prompt.

---

## Features

- Create, delete, and rename local user accounts
- Change user passwords
- List all local users
- Check if a user exists
- View detailed information about users
- Manage user group memberships (add/remove users to/from groups)
- Automatic Windows administrator privilege elevation on launch

---

## Requirements

- Windows operating system
- Python 3.6 or higher
- Run the script with administrator privileges for full functionality

---

## Usage

1. Open the dist folder containing the .exe



